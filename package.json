{
  "name": "billing-system",
  "version": "1.0.0",
  "description": "Secure POS Inventory Management System",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm run dev",
    "electron": "cd electron && npm start",
    "start:electron": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run electron\"",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:electron": "cd electron && npm run package",
    "build": "npm run build:frontend && npm run build:backend && npm run build:electron",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../electron && npm install && cd ../admin-portal && npm install && cd ../licensing-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
